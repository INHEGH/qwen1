{
  "name": "qwen1",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A Cloudflare Worker for managing D1 database",
  "dependencies": {
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231205.0",
    "wrangler": "^3.0.0"
  }
}
